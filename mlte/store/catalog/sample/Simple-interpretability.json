{
    "header": {
        "identifier": "Simple-interpretability",
        "creator": "admin",
        "created": 1762969423,
        "updater": null,
        "updated": 1762969423,
        "catalog_id": "sample"
    },
    "tags": [
        "Classification"
    ],
    "quality_attribute": "Interpretability",
    "code": "def create_model_image(model_path):\n\t#Helper function to create an image of the decision tree model for this dataset.\n\tloaded_model = _load_model(model_path)\n\tiris = sk_datasets.load_iris(as_frame=True)\n\n\tfig = plt.figure(figsize=(12,12))\n\t_ = sk_tree.plot_tree(loaded_model, feature_names = iris.feature_names, class_names = iris.target_names, filled = True)\n\n\n\tfig.savefig(IMAGE_PATH_INTER)\n\n\treturn IMAGE_PATH_INTER\n\n",
    "description":"Generates a diagram of a decision tree that should be manually validated to ensure the diagram is interperable and useful in mapping model inut values to the output classfication ",
    "inputs": "A path to  a sci-kit learn decision tree classifier ",
    "output": "Tree diagram that should be manually validated.  The diagram should clearly map the decision tree classifer's input values to the output classfiication."
}