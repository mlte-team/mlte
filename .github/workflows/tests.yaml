name: Tests

on:
  - push
  - pull_request

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
        python-version: ['3.7', '3.8', '3.9']

    steps:
    - uses: actions/checkout@v2
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v2
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install tox tox-gh-actions importlib-metadata==4.13.0 black flake8 mypy types-requests
    - name: Format with black
      run: black --check src/ && black --check test/
    - name: Lint with flake8
      run: flake8 ---ignore E501,E503 src/ && flake8 --ignore E501,E503 test/
    - name: Typecheck with mypy
      run: mypy src/ && mypy test/
    - name: Test with tox
      run: tox --develop